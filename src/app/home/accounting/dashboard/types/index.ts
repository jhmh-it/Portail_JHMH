/**
 * Export barrel pour les types du dashboard accounting
 */

export type {
  FilterState,
  DashboardMetrics,
  DashboardMetricsResponse,
  DashboardAPIParams,
  ActifOption,
  DashboardErrorResponse,
  DashboardTabProps,
} from './dashboard';

// Note: ActifListing importé depuis global types si nécessaire
